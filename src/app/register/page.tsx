import { RegisterClient } from "./register-client";

export const metadata = {
  title: "Register | Shade",
  description: "Complete your Shade merchant profile.",
};

export default function RegisterPage() {
  return <RegisterClient />;
}
